server:
  port: 8084

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: medical-stock-service
          uri: lb://medical-stock-service
          predicates:
            - Path=/stock/**

        - id: authorization-service
          uri: lb://authorization-service
          predicates:
            - Path=/auth/**

        - id: schedule-service
          uri: lb://schedule-service
          predicates:
            - Path=/schedule/**

        - id: supply-service
          uri: lb://supply-service
          predicates:
            - Path=/supply/**


eureka:
  instance:
    hostname: localhost
